import styled from "@emotion/styled";

export const FooterRoot = styled.footer`
  padding: 30px;
  border-top: solid var(--sub-border-color);
  font-size: 14px;

  width: 100%;
  text-align: center;
`;
